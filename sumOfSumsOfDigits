class UserMainCode {
    public int sumOfSumsOfDigits(int input1) {
        String numStr = Integer.toString(input1);
        int totalSum = 0;

        for (int i = 0; i < numStr.length(); i++) {
            int partialSum = 0;
            for (int j = i; j < numStr.length(); j++) {
                partialSum += Character.getNumericValue(numStr.charAt(j));
            }
            totalSum += partialSum;
        }

        return totalSum;
    }
}
